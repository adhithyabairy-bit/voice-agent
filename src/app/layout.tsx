import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "VoiceAI — AI Voice Agents for Indian Businesses",
  description: "Talk to your customers in Telugu, Hindi, English and more — naturally, instantly, and 24/7. Real-time multilingual AI voice agent platform.",
  keywords: ["AI voice agent", "Indian languages", "Telugu", "Hindi", "voice assistant", "business AI"],
};

import { AuthProvider } from "@/lib/auth/auth-context";

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
